import React from "react";
import "./Header.css";

const Header = props => <h1 className="header">Clicky Game!

<h3> Click on an image to earn points, but don't click on any more than once!</h3></h1>
;

export default Header;